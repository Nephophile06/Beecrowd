#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

int main() 
{
    double a, b, c;
    
    while (cin >> a >> b >> c) 
    {
        double s = (a + b + c) / 2.0;
        double A = sqrt(s * (s - a) * (s - b) * (s - c));  
        double R = (a * b * c) / (4.0 * A);
        double r = A / s;  

        cout << fixed << setprecision(4)
             << (M_PI * R * R - A) << " "  
             << (A - M_PI * r * r) << " "  
             << (M_PI * r * r) << endl;    
    }
    return 0;
}
