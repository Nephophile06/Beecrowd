#include <stdio.h>
#include<math.h>
int main() 
{
    double x1,x2,y1,y2,distance,p1,p2;
    scanf("%lf",&x1);
    scanf("%lf",&y1);
    scanf("%lf",&x2);
    scanf("%lf",&y2);
    p1=pow((x2-x1),2);
    p2=pow((y2-y1),2);
    distance=sqrt(p1+p2);
    printf("%.4lf\n",distance);
    return 0;
}
