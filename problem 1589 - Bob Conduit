#include<stdio.h>
int main()
{
    int T, R1, R2, r;
    scanf("%d", &T);

    for (int i = 0; i < T; i++)
    {
        scanf("%d %d", &R1, &R2);
        r = R1 + R2;
        printf("%d\n", r);
    }
}
