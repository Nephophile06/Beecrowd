#include <stdio.h>
int main() 
{
    int time, speed, distance;
    float litre;
    scanf("%d %d", &time, &speed);
    distance = time * speed;
    litre = distance / 12.0;
    printf("%.3f\n", litre);
    return 0;
}
