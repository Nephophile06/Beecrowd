#include <stdio.h>

int main()
{
    double arr[100], X;
    scanf("%lf", &X);

    for (int i = 0; i < 100; i++)
    {
        arr[i] = X;
        printf("N[%d] = %.4lf\n", i, arr[i]);
        X = X / 2.0;
    }

    return 0;
}
