#include <stdio.h>

int main()
{
    int arr[20];
    int i = 0, j = 0;

    for (i = 0; i < 20; i++)
    {
        scanf("%d", &arr[i]);
    }

    i = 0;
    for (j = 19; j >= 0; j--)
    {
        printf("N[%d] = %d\n", i, arr[j]);
        i++;
    }

    return 0;
}
