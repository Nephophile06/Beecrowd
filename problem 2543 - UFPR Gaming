#include <iostream>
using namespace std;

int main() 
{
    int m, n;
    int testCase = 1;

    while (cin >> m >> n) 
    {
        int count = 0;

        for (int i = 0; i < m; ++i) 
        {
            int id, type;
            cin >> id >> type;

            if (id == n && type == 0) 
                count++;
        }

        cout << count << endl;
    }

    return 0;
}
