#include <stdio.h>

int main()
{
    int a;
    scanf("%d", &a);
    int arr[10];

    for (int i = 0; i < 10; i++)
    {
        arr[i] = a;
        printf("N[%d] = %d\n", i, arr[i]);
        a = a * 2;
    }
    
    return 0;
}
