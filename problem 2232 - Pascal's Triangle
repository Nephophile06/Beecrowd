#include <iostream>
#include <cmath> 
using namespace std;

int main() 
{
    int T; 
    cin >> T;
    
    while (T--) 
    {
        int N;
        cin >> N;
        long long sum = (1LL << N) - 1; 
        cout << sum << endl;
    }
    
    return 0;
}
