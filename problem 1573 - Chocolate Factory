#include <iostream>
#include <cmath>
using namespace std;

int main() 
{
    int A, B, C;

    while (cin >> A >> B >> C, A || B || C) 
    {
        int volume = A * B * C;
        int edge = cbrt(volume);

        cout << edge << endl;
    }

    return 0;
}
