#include <stdio.h>

int main()
{
    int x;
    scanf("%d", &x);
    int odd;

    if (x % 2 == 0)
        odd = x + 1; 
    else
        odd = x; 

    for (int i = 0; i < 6; i++)
    {
        printf("%d\n", odd);
        odd = odd + 2;
    }

    return 0;
}
